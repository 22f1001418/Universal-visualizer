[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Health check — Railway uses this to confirm the app booted
healthcheckPath = "/healthz"
healthcheckTimeout = 30
